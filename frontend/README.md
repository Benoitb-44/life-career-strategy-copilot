# Frontend (Next.js minimal)

## Run

```bash
npm install
npm run dev
```

By default the app runs in mock backend mode.

Environment variables:

- `NEXT_PUBLIC_USE_MOCK=true` (default)
- `NEXT_PUBLIC_API_BASE_URL=http://localhost:8000`
